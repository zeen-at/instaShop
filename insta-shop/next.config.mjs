/** @type {import('next').NextConfig} */
const nextConfig = {
    // output: "export",

    //  For SSG, One would need to generate static props and static Params in order to build
};

export default nextConfig;
